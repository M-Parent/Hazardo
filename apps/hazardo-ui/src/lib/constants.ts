export const TIME_OPTIONS = ["AM", "PM", "Night", "Mixed"] as const;
export const VIBE_OPTIONS = ["Friend", "Date", "Family", "Mixed"] as const;

/** Maximum image file size in bytes (5 MB) */
export const MAX_IMAGE_SIZE = 5 * 1024 * 1024;
